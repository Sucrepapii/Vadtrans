# Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/vadtrans
# For production, use MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vadtrans?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=vadtrans_secret_key_2024
JWT_EXPIRE=7d

# Frontend URL (for CORS and email links)
CLIENT_URL=http://localhost:3000
# For production:
# CLIENT_URL=https://your-vercel-frontend-url.vercel.app

# Email Configuration (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=no-reply@vadtrans.com
SUPPORT_EMAIL=support@vadtrans.com

# Note: For Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an App Password at https://myaccount.google.com/apppasswords
# 3. Use the App Password as SMTP_PASS
